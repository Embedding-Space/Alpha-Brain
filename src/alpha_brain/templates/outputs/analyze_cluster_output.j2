Current time: {{ current_time | format_time_full }}

## Cluster {{ cluster_number }} of {{ total_clusters }}

**{{ memory_count }} memories** • Similarity: {{ "%.3f"|format(similarity) }}  
Time span: {{ oldest }} to {{ newest }}

### Cluster Metrics
- **Radius**: {{ "%.4f"|format(radius) if radius else "N/A" }} (max distance from center)
- **Density σ**: {{ "%.4f"|format(density_std) if density_std else "N/A" }} (clustering tightness)
- **Interestingness**: {{ "%.4f"|format(interestingness_score) if interestingness_score else "N/A" }} (size × tightness)

---

{% for memory in memories %}
### Memory {{ loop.index }} of {{ memory_count }}

**ID**: {{ memory.id }}  
**Created**: {{ memory.created_at }}

**Content**:
{{ memory.content }}

{% if memory.marginalia %}**Metadata**:
- Summary: {{ memory.marginalia.summary | default("N/A") }}
- Importance: {{ memory.marginalia.importance | default("N/A") }}
- Keywords: {{ memory.marginalia.keywords | join(", ") if memory.marginalia.keywords else "N/A" }}
{% endif %}

---

{% endfor %}